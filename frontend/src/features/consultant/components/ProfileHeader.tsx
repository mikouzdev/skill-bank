import { Box, Typography } from "@mui/material";

export default function ProfileHeader() {
  return (
    <Box sx={{ p: 2, border: 1 }}>
      <Typography>Consultant Profile Header</Typography>
    </Box>
  );
}
