default:
  image: node

cache:
  paths:
    - node_modules/

stages:
  - install
  - lint
  - build
  - deploy

install:
  stage: install
  script:
    - npm install -g pnpm
    - cd backend
    - pnpm install
    - cd ..
    - cd frontend
    - npm install -g pnpm
    - pnpm install
  #- pnpm run db:migrate (database has to be running)
    #- pnpm run db:seed

lint:
  stage: lint
  script:
    - npm install -g pnpm
    - pnpm run lint
    - cd ..
    - cd backend
    - pnpm run lint

