trigger:  
  branches:  
       include:
       - main

jobs:  
  - job: Build  
          pool:  
          vmImage: 'ubuntu-latest'
          steps:  
          - script: echo "Building application"
  - job: Lint_backend
          pool:  
          vmImage: 'ubuntu-latest'
          steps:  
          - script: cd backend
          npm install -g pnpm
          pnpm install
          npm run lint
