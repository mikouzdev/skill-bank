default:
  image: node

cache:
  paths:
    - node_modules/

stages:
  - install
  - lint
  - build
  - deploy

install:
  stage: install
  script: 
    - cd backend
    - npm install pnpm
    - pnpm install
    - pnpm run db:migrate (database has to be running)
    - pnpm run db:seed
